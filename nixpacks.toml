[phases.setup]
nixPkgs = ["php84", "composer", "nodejs"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader"
]

[phases.build]
cmds = [
  "npm install",
  "npm run build",
  "php artisan config:clear"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"